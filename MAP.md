# Mâm An implementation map

Local-first React/TypeScript/Vite prototype: image/sample → editable catalog estimate → atomic local snapshot → glucose → seven-day report.

Boundaries: features → application ports/use cases → pure domain. Composition root injects catalog, Dexie, image and AI adapters. API → server services → provider ports. No health-history server or auth.

Frontier: bootstrap on codex/implementation-bootstrap; source contracts read in PRD → SRS → HLD → LLD order. Deterministic flow precedes live providers.

Decisions: npm (available Node 24/npm 11); plain CSS; generated catalog committed and reproducibly checked. LLD overrides HLD provider default. No changes to baseline documents.
